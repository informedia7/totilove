-- Migration: Add user_data JSONB column to deleted_user_placeholders table
-- This allows storing full user data when a user is deleted

-- Add the user_data column if it doesn't exist
ALTER TABLE deleted_user_placeholders 
ADD COLUMN IF NOT EXISTS user_data JSONB;

-- Add index on user_data for faster queries (optional, but helpful for lookups)
CREATE INDEX IF NOT EXISTS idx_deleted_user_placeholders_user_data 
ON deleted_user_placeholders USING GIN (user_data);

-- Add comment to document the column
COMMENT ON COLUMN deleted_user_placeholders.user_data IS 'Stores complete user data from users table when user is deleted';
















































































































































































































